# Delivery Note: Packet 0006 cli-v1

**PR**: https://github.com/GalaxyRuler/apython/pull/7
**Branch**: `packet/0006-cli-v1`
**Implementation commit**: 5c0e8f39ff17af7b0b4520f625a57732d3de8694
**Implementer**: Gemini 3.1 Pro
**Reviewer**: Claude

## What shipped — files created, key implementation choices

- `arabicpython/cli.py`: Implemented the `main` entry point using `argparse`. 
    - Used `argparse.REMAINDER` for the `args` positional argument to correctly capture all arguments after the file or `-c` code.
    - Wrapped `parser.parse_args()` in a `try-except SystemExit` block to return the exit code as an integer, improving testability.
    - Implemented a custom `__main__` namespace setup that mirrors Python's `runpy` behavior, ensuring `__file__`, `__name__`, and `__builtins__` are correctly set for the executed code.
    - Added a `if __name__ == "__main__": sys.exit(main())` block to allow running the module via `python -m arabicpython.cli`.
- `tests/test_cli.py`: Implemented all 29 required tests, covering help/version, file mode, inline mode, stdin mode, execution semantics, error reporting, and subprocess integration.
- `examples/hello.apy`: Created a minimal example using the actual `اطبع` mapping for `print`.

## Deviations from the spec — anything you did differently and why; "None" if verbatim

- **None.** The implementation follows the spec verbatim.

## Implementation notes worth remembering — non-obvious decisions

- **Collision Mitigation in Tests**: In `test_run_class_with_method`, the common Arabic letter `ك` was initially used as a variable name but failed because it maps to the Python keyword `as` (via `كـ` normalization). I changed the variable name to `كائن` (object) to avoid this syntax error.
- **Path Escaping in Tests**: On Windows, `repr(list)` escapes backslashes in paths. I updated `test_sys_argv_in_file_mode` to construct the expected string using `str([path, ...])` which correctly accounts for this escaping when compared against `capsys` output.
- **BOM Handling**: I chose to rely on `translate` (and its internal `tokenize` call) to handle the UTF-8 BOM. Added a test case `test_utf8_with_bom_handled` to verify this works correctly.

## Validation — what you ran and the result

- `python -m pytest -v`: All 226 tests passed (including the 29 new CLI tests and all previous regression tests).
- `python -m ruff check .`: 0 errors.
- `python -m black --check .`: Passes.
- `pip install -e .` followed by `apython examples/hello.apy`: Verified manually that it prints "مرحبا، يا عالم".

## Open questions for the planner — anything ambiguous in the spec

None.

## Planner addendum (2026-04-18, post-merge)

Merged as squash commit [`d5b4861`](https://github.com/GalaxyRuler/apython/commit/d5b4861). All 9 CI cells green on first push; this is the first packet to land without a CI fix-up cycle. Kudos.

Two minor non-blocking nits noted during review (intentionally not requested as a round-trip):

- `arabicpython/cli.py:39-42` — a four-line dev-thinking comment block ("Use parse_known_args to avoid... Actually, the spec says...") was left in. Will get cleaned up in the next cli touch.
- `arabicpython/cli.py:128-133` — two adjacent `except` blocks both call `traceback.print_exc()` and `return 1`; could collapse to a single `except Exception:`. Cosmetic.

Implementer flagged a real dictionary footgun in their notes worth surfacing here: the Arabic letter `ك` alone normalizes to the Python keyword `as`. This is correct per the dictionary (`كـ` → `as` as the "as a/like" particle) but means single-letter `ك` identifiers are unusable. Test fixtures should use multi-letter Arabic names (the implementer used `كائن` for "object"). Worth a documentation note in the `ar-v1` dictionary header eventually; not urgent.
