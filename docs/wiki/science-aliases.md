<div dir="rtl">

# مكتبات علمية وبيانات — Science & Data Aliases

لغة الثعبان توفر أسماء عربية لأبرز مكتبات علم البيانات والحوسبة العلمية.

---

## جدول المكتبات العلمية

| الاسم العربي | Python | الوصف |
|-------------|--------|--------|
| `نمباي` | `numpy` | الحوسبة العددية والمصفوفات متعددة الأبعاد |
| `بانداس` | `pandas` | تحليل البيانات والجداول |
| `رسوم_بيانيه` | `matplotlib` | رسم المخططات عبر matplotlib |
| `سيبورن` | `seaborn` | الرسوم البيانية الإحصائية |
| `سايباي` | `scipy` | الخوارزميات العلمية والرياضية |
| `بيلو` | `PIL.Image` | معالجة الصور (Pillow) |

> الأسماء المعتمدة تأتي من `lexicon/libraries.toml`. بقيت بعض الأسماء القديمة كاستيراد توافقي عند عدم وجود تعارض، مثل `رسم_مخططات` لـ`matplotlib` و`صور` لـ`PIL.Image`.

---

## نمباي (`numpy`)

### الاستيراد

```python
استورد نمباي باسم نم
```

### إنشاء المصفوفات

| Python | العربية | مثال |
|--------|---------|-------|
| `array` | `مصفوفه` | `نم.مصفوفه([1, 2, 3])` |
| `zeros` | `اصفار` | `نم.اصفار((3, 3))` |
| `ones` | `وحدات` | `نم.وحدات((2, 4))` |
| `empty` | `فارغه` | `نم.فارغه((5,))` |
| `full` | `ممتلئه` | `نم.ممتلئه((3, 3), 7)` |
| `arange` | `مدي` | `نم.مدي(0, 10, 2)` |
| `linspace` | `فضاء_خطي` | `نم.فضاء_خطي(0, 1, 50)` |
| `eye` | `عين` | `نم.عين(3)` |
| `reshape` | `اعد_شكل` | `نم.مصفوفه([1,2,3,4]).اعد_شكل(2,2)` |

### العمليات الرياضية

| Python | العربية |
|--------|---------|
| `sum` | `مجموع` |
| `mean` | `متوسط` |
| `std` | `انحراف_معياري` |
| `var` | `تشتت` |
| `max` | `الاكبر_صفيف` |
| `min` | `الاصغر_صفيف` |
| `dot` | `ضرب_نقطي` |
| `sqrt` | `جذر_تربيعي` |
| `abs` | `مطلق_صفيف` |
| `sort` | `رتب_صفيف` |
| `unique` | `فريد` |
| `where` | `حيث` |

### مثال كامل

```python
استورد نمباي باسم نم

# إنشاء مصفوفة
بيانات = نم.مصفوفه([[1, 2, 3], [4, 5, 6], [7, 8, 9]])

اطبع("الشكل:", بيانات.شكل)
اطبع("المتوسط:", نم.متوسط(بيانات, axis=0))
اطبع("الانحراف المعياري:", نم.انحراف_معياري(بيانات, axis=0))

# ضرب المصفوفات
أ = نم.مصفوفه([[1, 2], [3, 4]])
ب = نم.مصفوفه([[5, 6], [7, 8]])
جـ = نم.ضرب_نقطي(أ, ب)
اطبع("حاصل الضرب:\n", جـ)
```

---

## بانداس (`pandas`)

### الاستيراد

```python
استورد بانداس باسم بد
```

### الهياكل الأساسية

| Python | العربية | مثال |
|--------|---------|-------|
| `DataFrame` | `اطار_بيانات` | `بد.اطار_بيانات({"أ": [1,2,3]})` |
| `Series` | `سلسله_بيانات` | `بد.سلسله_بيانات([1, 2, 3])` |
| `Index` | `فهرس` | `بد.فهرس(["أ", "ب", "جـ"])` |
| `MultiIndex` | `فهرس_متعدد` | — |

### قراءة البيانات

| Python | العربية |
|--------|---------|
| `read_csv` | `اقرا_csv` |
| `read_excel` | `اقرا_اكسل` |
| `read_json` | `اقرا_json` |
| `read_sql` | `اقرا_sql` |
| `read_parquet` | `اقرا_parquet` |

### دمج البيانات

| Python | العربية |
|--------|---------|
| `concat` | `ادمج_بيانات` |
| `merge` | `اندمج` |
| `pivot_table` | `جدول_محوري` |

### مثال كامل

```python
استورد بانداس باسم بد
استورد نمباي باسم نم

# إنشاء جدول بيانات
موظفون = بد.اطار_بيانات({
    "الاسم": ["أحمد", "سارة", "محمد", "فاطمة"],
    "القسم": ["تقنية", "تسويق", "تقنية", "موارد_بشرية"],
    "الراتب": [8000, 6500, 9000, 7000],
    "سنوات_الخبرة": [3, 5, 7, 2]
})

# التصفية
تقنية = موظفون[موظفون["القسم"] == "تقنية"]
اطبع("قسم التقنية:\n", تقنية)

# التجميع
متوسط_بالقسم = موظفون.groupby("القسم")["الراتب"].mean()
اطبع("\nمتوسط الراتب بالقسم:\n", متوسط_بالقسم)

# الإحصاء الوصفي
اطبع("\nالإحصاء الوصفي:\n", موظفون["الراتب"].describe())
```

---

## سيبورن (`seaborn`)

### الاستيراد

```python
استورد سيبورن باسم رسم
```

### أنواع الرسوم

| Python | العربية | الوصف |
|--------|---------|--------|
| `lineplot` | `خط_بياني` | رسم الخط |
| `scatterplot` | `مخطط_نقاط` | رسم النقاط |
| `histplot` | `توزيع_بيانات` | التوزيع التكراري |
| `kdeplot` | `كثافه_احتماليه` | تقدير كثافة الاحتمال |
| `boxplot` | `مخطط_صندوق` | مخطط الصندوق والشعيرات |
| `violinplot` | `مخطط_كمان` | مخطط الكمان |
| `barplot` | `مخطط_اعمده` | الأعمدة البيانية |
| `heatmap` | `خريطه_حراره` | الخريطة الحرارية |
| `pairplot` | `شبكه_زوجيه` | مصفوفة الرسوم الثنائية |
| `set_style` | `ضبط_نمط` | تغيير نمط الرسم |
| `set_palette` | `ضبط_لوحه_الوان` | تغيير لوحة الألوان |

### مثال كامل

```python
استورد سيبورن باسم رسم
استورد بانداس باسم بد
استورد رسوم_بيانيه باسم مخططات

# تحميل بيانات تجريبية
بيانات_تلاميذ = بد.اطار_بيانات({
    "الدرجة": [65, 72, 88, 91, 55, 78, 83, 69, 94, 76],
    "الجنس": ["ذكر", "أنثى", "ذكر", "أنثى", "ذكر", "أنثى", "ذكر", "أنثى", "ذكر", "أنثى"],
    "المادة": ["رياضيات"] * 5 + ["علوم"] * 5
})

رسم.ضبط_نمط("whitegrid")

# رسم توزيع الدرجات
رسم.توزيع_بيانات(data=بيانات_تلاميذ, x="الدرجة", hue="المادة")
مخططات.عنوان("توزيع الدرجات")
مخططات.اعرض()
```

---

## سايباي (`scipy`)

### الاستيراد

```python
استورد سايباي باسم عح
```

### الوحدات الفرعية

| Python | العربية | الوصف |
|--------|---------|--------|
| `linalg` | `جبر_خطي_علمي` | الجبر الخطي المتقدم |
| `special` | `دالات_خاصه` | الدوال الرياضية الخاصة |
| `optimize` | `تحسين` | تحسين الدوال وإيجاد الجذور |
| `integrate` | `تكامل` | التكامل العددي |
| `signal` | `معالجه_اشارات` | معالجة الإشارات |
| `stats` | `احصاء_متقدم` | الاختبارات الإحصائية |
| `interpolate` | `استيفاء` | الاستيفاء العددي |
| `fft` | `تحويل_فورييه` | تحويل فورييه السريع |
| `sparse` | `مصفوفات_نادره` | المصفوفات المتفرقة |

### مثال

```python
استورد سايباي باسم عح

# إيجاد جذر دالة
دالة مربع_ناقص_4(س):
    ارجع س**2 - 4

جذر = عح.تحسين.brentq(مربع_ناقص_4, 0, 3)
اطبع(f"جذر المعادلة س²-4=0: {جذر}")  # 2.0

# اختبار t
مجموعه_1 = [23, 25, 28, 22, 27]
مجموعه_2 = [30, 32, 28, 35, 31]
t_stat, p_value = عح.احصاء_متقدم.ttest_ind(مجموعه_1, مجموعه_2)
اطبع(f"قيمة t: {t_stat:.3f}, قيمة p: {p_value:.3f}")
```

---

## بيلو (`PIL.Image`)

### الاستيراد

```python
استورد بيلو
```

### الوظائف الرئيسية

| Python | العربية | الوصف |
|--------|---------|--------|
| `open` | `افتح_صوره` | فتح ملف صورة |
| `new` | `صوره_جديده` | إنشاء صورة جديدة |
| `save` | `احفظ` | حفظ الصورة |
| `show` | `اعرض` | عرض الصورة |
| `LANCZOS` | `تحجيم_عالي_جوده` | خوارزمية تحجيم عالية الجودة |
| `BICUBIC` | `تحجيم_تكعيبي` | تحجيم ثنائي التكعيب |
| `BILINEAR` | `تحجيم_خطي` | تحجيم ثنائي الخطي |

### مثال

```python
استورد بيلو

# فتح صورة وتحجيمها
صوره = بيلو.افتح_صوره("صورتي.jpg")
اطبع(f"الأبعاد: {صوره.size}")
اطبع(f"الوضع: {صوره.mode}")

# تحجيم
صوره_مصغره = صوره.resize((200, 200), بيلو.تحجيم_عالي_جوده)
صوره_مصغره.save("مصغره.jpg")

# تحويل لتدرج رمادي
رمادي = صوره.convert("L")
رمادي.save("رمادي.jpg")
```

</div>

---

# Science & Data Aliases (English summary)

| Arabic import | Python module | Purpose |
|---|---|---|
| `نمباي` | `numpy` | N-dimensional arrays, linear algebra, FFT |
| `بانداس` | `pandas` | DataFrames, CSV/Excel I/O, groupby |
| `رسوم_بيانيه` | `matplotlib` | Plotting and pyplot surface |
| `سيبورن` | `seaborn` | Statistical visualisation on top of matplotlib |
| `سايباي` | `scipy` | Optimize, integrate, signal, stats |
| `بيلو` | `PIL.Image` | Image open/save/resize (Pillow) |

All imports use `استورد <arabic_name>`. Aliases are resolved by the alias runtime and generated from `lexicon/libraries.toml`. These packages must be installed separately.
